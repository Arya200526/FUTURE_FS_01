body {
    margin: 0;
    font-family: Arial;
    background: #f4f6fb;
}

.container {
    display: flex;
}

/* Sidebar */
.sidebar {
    width: 220px;
    height: 100vh;
    background: linear-gradient(180deg, #8e6dfd, #6a5acd);
    color: white;
    padding: 20px;
}

.sidebar ul {
    list-style: none;
    padding: 0;
}

.sidebar li {
    padding: 10px;
    margin: 10px 0;
    cursor: pointer;
}

/* Main */
.main {
    flex: 1;
    padding: 20px;
}

/* Header */
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.header input {
    padding: 8px;
    width: 200px;
}

.profile {
    display: flex;
    align-items: center;
}

.profile img {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid white;
    box-shadow: 0 0 10px rgba(0,0,0,0.3);
}

/* Welcome */
.welcome {
    background: linear-gradient(90deg, #8e6dfd, #b388ff);
    color: white;
    padding: 20px;
    border-radius: 10px;
    margin: 20px 0;
}

/* Cards */
.cards {
    display: flex;
    gap: 20px;
}

.card {
    background: white;
    padding: 20px;
    flex: 1;
    border-radius: 10px;
    box-shadow: 0 0 5px rgba(0,0,0,0.1);
}

/* Section */
.section {
    margin-top: 20px;
}

.course {
    background: white;
    padding: 10px;
    margin: 10px 0;
    border-radius: 8px;
}